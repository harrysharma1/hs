{
    "dates": [
    {{- $pages := site.RegularPages -}}
    {{- range $index, $page := $pages -}}
      {{- if $index }},{{ end -}}
      
        "{{ $page.Date.Format "2006-01-02" }}"
      
    {{- end -}}
  ]
}